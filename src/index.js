'use strict';

module.exports = {
  register({ strapi }) {
    console.log("✅ [src/index.js] register çalıştı");
  },

  bootstrap({ strapi }) {
    console.log("✅ [src/index.js] bootstrap çalıştı");
  },
};
