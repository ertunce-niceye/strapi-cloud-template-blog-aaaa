module.exports = {
    async beforeCreate(event) {
        // Validation removed to allow creating Company without Team initially
    },

    async beforeUpdate(event) {
        // Validation removed
    },
};
